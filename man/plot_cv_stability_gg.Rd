% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_helpers.R
\name{plot_cv_stability_gg}
\alias{plot_cv_stability_gg}
\title{ggplot2-based CV ranking stability plot}
\usage{
plot_cv_stability_gg(cv_obj, metric = c("top1_frequency", "mean_rank"))
}
\arguments{
\item{cv_obj}{A list returned by \code{\link{cv_ranking_stability}}.}

\item{metric}{One of \code{"top1_frequency"} (default) or
\code{"mean_rank"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a \pkg{ggplot2} horizontal bar chart of either the top-1 frequency
or the mean rank of each candidate model from a repeated cross-validation
stability run.
}
\examples{
\dontrun{
models <- list(m1 = mpg ~ wt + hp, m2 = mpg ~ wt + hp + disp)
cv <- cv_ranking_stability(models, mtcars, v = 5, R = 20)
plot_cv_stability_gg(cv, "top1_frequency")
plot_cv_stability_gg(cv, "mean_rank")
}

}
