% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_helpers.R
\name{plot_stability_gg}
\alias{plot_stability_gg}
\title{ggplot2-based stability plot}
\usage{
plot_stability_gg(diag_obj, type = c("coefficient", "selection"))
}
\arguments{
\item{diag_obj}{A \code{"reprostat"} object returned by
\code{\link{run_diagnostics}}.}

\item{type}{One of \code{"coefficient"} (default) or \code{"selection"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a \pkg{ggplot2} bar chart for either the coefficient variance or
the selection frequency of each predictor term, ordered from lowest to
highest value.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  d <- run_diagnostics(mpg ~ wt + hp, mtcars, B = 50)
  plot_stability_gg(d, "coefficient")
  plot_stability_gg(d, "selection")
}
}

}
